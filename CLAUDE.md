# CLAUDE.md — healthyplan(헬씨플랜) 작업 인계 문서

새 세션(대화)을 열 때 이 파일을 먼저 읽는다. 사용자(송정호 박사님)는 기억이 짧아,
이전 대화 내용을 다시 설명하지 않아도 여기서 상태를 파악해야 한다.

> ## ★ 새 세션이 가장 먼저 할 일
>
> 1. **배포 상태부터 확인**: 라이브 주소 https://powercm77-git.github.io/healthyplan/ 가
>    실제로 뜨는지(`curl -s -o /dev/null -w "%{http_code}" ...`), `git status`로 로컬-원격
>    동기화 여부.
> 2. **★★ 이 저장소를 절대 비공개(private)로 전환하지 말 것.** 2026-08-18에 실제로
>    시도했다가 GitHub Pages 사이트가 죽는 장애가 났다(자세한 경위·복구법은
>    `../biz-eng-map/CLAUDE.md`의 2-3절 — 같은 계정, 같은 원인이라 그쪽에 기록해뒀다).
>    다시 시도하면 똑같이 죽는다.
> 3. **테스트부터 돌려서 현재 상태 확인**: `npm test` (vitest, 552개 — 전부 통과해야 정상).
> 4. 배포는 **GitHub Pages(workflow 빌드, GitHub Actions로 빌드→배포)다. Netlify 아님.**

---

## 1. 프로젝트가 뭔가

**헬씨플랜** — 식단·운동을 관리하는 개인용 건강관리 PWA(Progressive Web App).
React 19 + Vite 8 + Tailwind 4. 정적 사이트로 GitHub Pages 배포, **백엔드 서버 없음** —
모든 사용자 데이터는 브라우저 IndexedDB(`src/lib/db.js`, `idb` 라이브러리)에 로컬로만
저장된다. 오프라인 사용 가능(`vite-plugin-pwa`로 서비스워커 등록).

biz-eng-map과 마찬가지로 **학과(IT융합컴퓨터과) 관련 프로젝트로 보인다** — 커밋 메시지에
"발주자 지시 반영" 표현이 나오는 걸 보아 정해진 요구사항에 맞춰 만드는 중인 것으로 추정.
정확한 사업명은 사용자에게 확인 필요(미확인 — 아래 "물어봐야 하는 것" 참고).

### 화면 구성
최초 실행 시 `Onboarding.jsx`(프로필 입력) → 이후 하단 탭 4개(`TabBar.jsx`):
- 🏠 **홈** — 대시보드(`Home.jsx`)
- 🥗 **식단** — 오늘/주간/월간(`Meals.jsx` + `screens/meals/*`)
- 🏃 **운동** — 라이브러리/상세/실행(`Exercise.jsx` + `screens/exercise/*`)
- 📈 **리포트** — **잠김("리포트는 다음 단계에서 열려요")** — 아직 미구현, 다음 단계 기능
- ⚙️ 설정(`Settings.jsx`)은 탭바에 없고 홈에서 별도 진입

### 식단 기능
- 음식 DB 745개+(`src/data/foods.json`), 초성(ㄱㅅㅌ) 검색 지원(`src/lib/chosung.js`) —
  한글 IME 조합 중 검색이 깨지는 버그, NFD 유니코드 입력 문제 등을 과거에 여러 번 고쳤다
  (커밋 이력 `e2dfeb3`, `12c260f` 등).
- **3단계 주간 식단 자동 편성**(`src/lib/mealPlanner.js`, 2026-08-15 추가) — 규칙 기반 편성:
  아침 단백질 보장, 간식 가공우유 주 2회 상한, 끼니 구성 개수 상한(반찬 2·총 4),
  해장국류 주 1회 상한 + 국 다양성, 평일/주말 조리 난이도 배치. 실제 표본 검토로
  튜닝됨(도시락류-국·반찬 재조합 버그도 그 과정에서 발견·수정).
- 장보기 목록 자동 생성(`src/lib/groceryList.js`).

### 운동 기능
- 운동 라이브러리 **764종**(`src/data/exercises.json`) — **국민체력100(KSPO) 공공데이터
  재사용**. 처음 96종에서 2.6단계에 764종으로 확장(`32e211e`).
- **스틱 피겨 SVG 애니메이션을 직접 제작**(`src/components/exercise-animations/`) — 관절이
  이어진 하나의 골격으로 재제작(`f2ef73a`), 스쿼트·런지·푸시업 등 자세 사실성을 여러 차례
  다듬음(최근 커밋 다수가 이 튜닝 작업). 20개 포즈 최종 설계값 적용 완료(`c5acdcc`, 8/15).
- 운동 영상(KSPO 원본) 매칭 — 위험한 오매칭(운동과 안 맞는 영상)을 찾아 제거한 이력 있음
  (`b9d6341`) — **영상 매칭은 자동화라 오매칭 가능성 항상 염두에 둘 것.**
- 운동 실행 중 실시간 동기부여(`ExerciseSession.jsx`) — 칼로리 소모, 음식 환산(이만큼
  운동하면 무슨 음식 칼로리랑 같다), 진행바, 성장 비교, 누적 성과(2.7단계, `f5c3935`).
- 루틴 자동 편성(`exerciseEngine.js`) — 목표 시간(예: 45분)에 맞게 세트·유산소 시간을
  배정. **개수만 늘리면 시간이 못 채워지는 버그**(45분 목표인데 37분에 그침)를 겪고
  세트·유산소 시간을 먼저 늘리는 방식으로 고침(`42e9d8d`).

### 데이터 원본 (`data-raw/`, 개발용 — 앱에는 안 실림)
KSPO API에서 받아온 원본 스냅샷. `kspo-videos.json`(34MB, 제일 큼), `kspo-library-plan.json`,
`kspo-exercise-candidates.json`, `kspo-programs.json`. `scripts/*.mjs`가 이 원본들을 받아오고
(`fetch-*`), 분류하고(`classify-*`), 최종 `src/data/exercises.json`으로 조립한다
(`build-exercise-library.mjs`, `finalize-library-v26.mjs`). **이 스크립트들은 로컬 빌드
도구일 뿐, 앱 실행 시 호출되지 않는다** — 최종 사용자는 미리 만들어둔 `exercises.json`만 씀.

## 2. 상태 (2026-08-18 기준, 첫 조사)

- **테스트 552개 전부 통과**(vitest, 6개 파일), 워킹 트리 클린, 최신 커밋 `bb40df1`(8/15).
- GitHub Pages(workflow 빌드)로 배포 중, 정상 작동 확인함(2026-08-18 — 비공개 전환
  사고 후 복구 과정에서 재확인. 2-3절 아래 "알아둘 것" 참고).
- 리포트 탭은 UI에 자리만 있고 잠겨 있음 — **다음 단계 기능, 아직 설계도 안 된 것으로 보임.**
- `.env`의 `DATA_GO_KR_KEY`는 `scripts/*.mjs`(로컬 빌드 스크립트)에서만 쓰이고
  `src/`(실제 브라우저에 배포되는 코드)에는 전혀 참조가 없음 — **키가 클라이언트 번들에
  노출되지 않는다는 것을 직접 확인함(2026-08-18).** biz-eng-map과 같은 패턴.
- 앱은 런타임에 외부 API를 전혀 호출하지 않는다(`src/`에 `fetch(` 없음) — 완전
  정적·오프라인 동작.

## 3. 알아둘 것

- **비공개 전환 금지** — 위 새 세션 안내 2번 참고. `biz-eng-map/CLAUDE.md` 2-3절에
  두 저장소 공통 원인·복구법이 기록돼 있다.
- **배포는 workflow 빌드 방식**(legacy 아님) — `.github/workflows/`의 "Deploy to GitHub
  Pages" 워크플로가 `npm run build` 후 `actions/deploy-pages`로 배포한다. Pages 설정
  자체가 사라지면(비공개 전환 등으로) `gh api repos/.../pages -X POST -f
  "build_type=workflow"`로 먼저 재생성한 다음 워크플로를 수동 실행
  (`gh workflow run "Deploy to GitHub Pages"`)해야 한다 — 단순 push만으론 복구 안 됨.
- **로컬 개발 서버**: `npm run dev`(Vite, 포트는 vite.config.js 확인). biz-eng-map처럼
  외부 지도 API 도메인 락 같은 제약은 없어 보임(런타임에 외부 호출이 없으므로).
- 데이터 재수집이 필요할 때만 `scripts/`의 `fetch-*.mjs`를 실행 — `.env`에
  `DATA_GO_KR_KEY` 필요(공공데이터포털에서 발급, data.go.kr).

## 4. 다음에 할 만한 것 (파악만 하고 아직 사용자와 우선순위 조율 안 함)

- **리포트 탭 구현** — 탭바에 자리만 있고 잠긴 상태. 무엇을 보여줄지(주간 요약? 체중
  추이? 목표 달성률?) 사용자 확인 필요.
- README가 없음 — 이 CLAUDE.md가 사실상 그 역할을 겸한다.
- 운동 영상 매칭 품질 — 과거 "위험한 오매칭"을 찾아 제거한 이력이 있는 만큼, 정기적으로
  표본 재검토가 필요할 수 있다(songstock의 "정기 재검증" 개념과 비슷).

## ❓ 사용자에게 물어봐야 하는 것 (아직 답 못 받음)

- [ ] 정확히 어떤 사업/과제의 산출물인지("발주자 지시"가 누구를 가리키는지) —
  biz-eng-map처럼 취업역량강화 사업 관련인지 확인 필요.
- [ ] 리포트 탭에 무엇을 넣을지.
